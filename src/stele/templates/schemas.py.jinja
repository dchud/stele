"""Generated by stele. Do not edit.

Schema *tokens*, not schema names. Generated models are bound to these; the
mapping from token to real schema is supplied per-engine via
``schema_translate_map``, which is what lets one set of classes address both
the Databricks catalog and the SQL Server replica.
"""

from stele.runtime import schema_token

{% for const, name in schemas %}
{{ const }} = schema_token("{{ name }}")
{% endfor %}

LOGICAL_SCHEMAS = [
{% for const, name in schemas %}
    "{{ name }}",
{% endfor %}
]
